#include <vector>
#include <cmath>
#include <algorithm>

void dataToBuffer(double data);
double getTotalAvg(std::vector<double> sig);
std::vector<double> sigInversion(std::vector<double> sig);
void segmentation();
void imsAlgorithm();
void PulseCalculation();
